<div class="logo">
<h1>Registro de Horas.</h1>
</div>
<% if current_user%>
  <ul class="custom-nav nav nav-pills">
    <li class="nav-item <%= active?(root_path) %>">
      <%= link_to '', root_path, class: 'glyphicon glyphicon-home' %>
    </li>
    <%= render partial: 'shared/employee_items' if employee?%>

    <%= render partial: 'shared/audit_logs' if admin? %>

    <li class="nav-item dropdown ml-md-auto pull-right">
      <a class="dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown">Opciones<strong class="caret"></strong></a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
        <%= render partial: 'shared/admin_dropdown' if admin?%>
        <div class="dropdown-divider"></div> 
        <%= link_to 'Cerrar Session', destroy_user_session_path, method: :delete %>
      </div>
    </li>
  </ul>
<% else%>
  <ul class="custom-nav nav nav-pills">
    <li class="nav-item dropdown ml-md-auto pull-right">
      <a class="nav-link dropdown-toggle" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown">Opciones</a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
        <%= link_to 'Login', new_user_session_path %>
      </div>
    </li>
  </ul>
<% end %>